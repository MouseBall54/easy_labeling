# UX 개선 작업 계획 및 진척도

기준 문서: [구현·UI 감사 보고서](./UX_IMPLEMENTATION_AUDIT_2026-09-12.md)
상태 범례: `Not started` / `In progress` / `Blocked` / `Done` / `Verified`

## 목표와 완료 기준

목표는 감사에서 확인한 사용자 흐름 단절을 줄이고, 변경 후에도 Detection·Segmentation·샘플·검수·자동화의 기존 기능을 보존하는 것이다.

전체 완료 기준:

- [x] 아래 P1/P2 작업이 각 항목의 수용 기준을 만족한다.
- [x] 결정이 필요한 항목은 승인된 결정과 근거가 문서에 남아 있다.
- [x] 변경한 흐름의 단위/E2E 테스트가 추가 또는 갱신돼 통과한다.
- [x] 기본 데스크톱과 최소 지원 폭에서 주요 흐름을 실제 실행으로 확인한다.
- [x] 전체 단위 테스트와 관련 E2E가 통과한다. 기존 실패가 남으면 원인·영향·재현 명령을 명시한다.

## 작업 순서

| 순서 | 작업 묶음 | 선행 조건 | 완료 산출물 |
| --- | --- | --- | --- |
| 0 | 기준선·결정 확정 | 없음 | 현 상태 재현, 테스트 실패 분류, 결정 기록 |
| 1 | 샘플 시작점·검수 대기열 | 0 | 일관된 샘플 진입과 검수 진입 흐름 |
| 2 | 자동화·주석 목록 | 1과 병행 가능 | 미리보기 동기화, 주석 식별성 개선 |
| 3 | 전처리·좁은 화면 정책 | 결정 D-01, D-02 필요 | 승인된 정보 구조·지원 정책 구현 |
| 4 | Segmentation 및 회귀 복구 | 0의 실패 분류 | 첫 클래스 정책과 오류/Undo 검증 |
| 5 | 통합 검증·문서 갱신 | 1~4 | 최종 테스트 결과와 사용자 흐름 확인 |

## 결정 기록

| ID | 결정할 내용 | 선택지 | 현재 상태 | 결정권자/근거 |
| --- | --- | --- | --- | --- |
| D-01 | 전처리 작업공간의 위치 | 독립 작업공간 유지 / Automation의 사전 단계로 통합 | `Decided` | 사용자 승인: A — 독립 작업공간 유지, Inspector 닫기 |
| D-02 | 좁은 화면 정책 | 최소 폭 미만 차단·안내 / 제한적 반응형 지원 | `Decided` | 사용자 승인: A — 800px 이하 제한적 반응형 지원 |
| D-03 | Segmentation 신규 이미지 기본 클래스 | 명시적 선택 강제 / 고정 기본값 + 안내 | `Decided` | 사용자 승인: A — 빈 마스크에서 명시적 클래스 선택 강제 |
| D-04 | 샘플의 첫 화면 목적 | 라벨된 데모 이미지 / 빈 이미지 안내 | `Decided` | 작업 지시의 기본안에 따라 `sample_1.jpg`와 52개 박스를 표시 |

### 결정 근거 및 구현 범위

| ID | 선택 A | 선택 B | 권장안과 현재 코드 근거 | 결정 후 최소 구현 범위 |
| --- | --- | --- | --- | --- |
| D-01 | 전처리를 독립 작업공간으로 유지하고 오른쪽 Inspector를 접는다 | Automation 작업공간 안의 사전 단계로 통합한다 | **A 권장.** 전처리는 이미 작업 레일과 전용 왼쪽 패널을 가지며 Detection·Segmentation 양쪽 입력 소스를 제어한다. 정보 구조를 합치면 범위가 커진다. | Preprocessing 진입 시 왼쪽 설정만 열고 오른쪽 Inspector를 닫는다. 패널 제목·설명에 처리 화면과 알고리즘 입력의 관계를 명시하고 전환 E2E를 추가한다. |
| D-02 | 폭 800px 이하의 좁은 데스크톱에서 제한적 반응형을 지원한다 | 최소 폭 미만을 작업 불가로 안내한다 | **A 권장.** CSS에 이미 800px 이하 오버레이 패널 구조와 640px 빈 화면 E2E가 있다. 현재 결함은 작업 전환 시 양쪽 패널에 동시에 `mobile-open`이 붙는 상태 관리다. 모바일 UA 차단 정책은 그대로 유지한다. | 한 번에 한 패널만 열고 캔버스를 기본 노출한다. 작업 레일로 패널을 전환하고 390×844·640×360·800px 경계 E2E를 추가한다. |
| D-03 | 빈 마스크에서는 그릴 클래스를 명시적으로 선택하기 전까지 페인트를 막는다 | 유효한 기본 클래스를 자동 선택하고 눈에 띄는 안내를 표시한다 | **A 권장.** 현재 문서는 클래스 정보 유무와 무관하게 내부 기본값 `1`을 사용하므로 잘못된 클래스 기록 위험이 있다. 명시 선택은 첫 입력의 의도를 가장 확실히 보장한다. | 빈 마스크에 선택 대기 상태를 추가한다. 클래스 정보가 없으면 클래스 파일 로드/생성 안내를 표시하고, 선택 후에만 Brush·Polygon·Superpixel 입력을 허용한다. 기존 마스크를 연 이미지는 저장된 상태를 유지한다. |

2026-09-12 사용자 회신 `D-01 A, D-02 A, D-03 A`에 따라 세 항목 모두 선택 A로 확정했다.

## 작업 백로그 및 진척도 체크

### UX-01 — 샘플 시작 이미지와 테스트 계약 정렬

- 우선순위: P1
- 상태: `Verified`
- 의존성: D-04
- 작업 체크:
  - [x] `Load Sample`의 의도된 첫 이미지를 확정한다.
  - [x] 시작 이미지·토스트·빈 상태의 메시지를 같은 목적에 맞춘다.
  - [x] 샘플 데이터 로드 순서 또는 선택 로직을 수정한다.
  - [x] 샘플·레이아웃·검수 E2E의 공통 초기 상태를 한 계약으로 정리한다.
- 수용 기준:
  - [x] 샘플 로드 직후 화면이 선택된 데모 목적과 일치한다.
  - [x] 라벨된 데모를 선택하면 `sample_1.jpg`와 52개 Detection 박스가 보인다.
  - [x] 관련 E2E가 같은 초기 상태를 사용하고 통과한다.

### UX-02 — Review 대기열과 현재 이미지 동기화

- 우선순위: P1
- 상태: `Verified`
- 의존성: UX-01
- 작업 체크:
  - [x] Review 진입 시 필터 대상과 현재 이미지의 관계를 정의한다.
  - [x] 현재 이미지가 필터 밖이면 첫 대기열 항목을 자동 선택한다.
  - [x] 대기열 위치·총 개수·비어 있음 상태를 명확히 표시한다.
  - [x] Review E2E에 진입·이동·상태 변경 시나리오를 추가한다.
- 수용 기준:
  - [x] `Review: Has issues`와 “No quality issues found”가 설명 없이 함께 나타나지 않는다.
  - [x] 대기열 이동 버튼은 현재 위치에 맞게 활성화된다.
  - [x] 검수 완료/되돌리기 후 목록과 Inspector가 일관되게 갱신된다.

### UX-03 — Automation 레이아웃 미리보기 동기화

- 우선순위: P2
- 상태: `Verified`
- 의존성: 없음
- 작업 체크:
  - [x] 선택된 레이아웃 복원 시점과 미리보기 생성 시점을 추적한다.
  - [x] 작업공간 진입 시 선택된 레이아웃 미리보기를 자동 생성한다.
  - [x] 동기식 로컬 렌더링이라 별도 로딩·재시도 상태가 필요하지 않음을 확인한다.
  - [x] Automation E2E를 갱신한다.
- 수용 기준:
  - [x] 이미 선택된 레이아웃에 “다시 선택”을 요구하지 않는다.
  - [x] Apply layout 전에 사용자가 현재 레이아웃을 확인할 수 있다.

### UX-04 — Detection 주석 목록 식별성 개선

- 우선순위: P2
- 상태: `Verified`
- 의존성: 없음
- 작업 체크:
  - [x] 목록 항목에 안정적 번호와 간단한 기하 정보를 표시한다.
  - [x] 클릭 및 키보드 Enter/Space 선택을 캔버스 강조와 연결한다.
  - [x] 좁은 Inspector에서 말줄임과 편집·삭제 버튼 접근성을 확인한다.
- 수용 기준:
  - [x] 동일 클래스 박스 두 개 이상을 목록에서 구분할 수 있다.
  - [x] 목록에서 선택한 항목이 캔버스에서 즉시 식별된다.

### UX-05 — 전처리 작업공간 정보 구조 정리

- 우선순위: P1
- 상태: `Verified`
- 의존성: D-01
- 작업 체크:
  - [x] D-01 결정을 기록한다.
  - [x] 전처리 화면에서 현재 작업과 무관한 Inspector 패널을 닫는다.
  - [x] 전처리 결과가 캔버스 미리보기이며 Detection Automation·Segmentation AI Select/Superpixel 입력 소스는 별도 선택임을 명시한다.
  - [x] 전환 E2E를 갱신한다.
- 수용 기준:
  - [x] 전처리 작업 중 Automation 실행 패널이 맥락 없이 남아 있지 않다.
  - [x] Inspector를 닫고 재확장 제어를 숨겨 현재 작업과 일치시킨다.

### UX-06 — 최소 화면 폭 정책 구현

- 우선순위: P1
- 상태: `Verified`
- 의존성: D-02
- 작업 체크:
  - [x] D-02와 반응형 전환 폭 800px를 기록한다.
  - [x] 모바일 UA 차단 정책은 유지하고, 좁은 데스크톱 뷰포트만 제한적으로 지원한다.
  - [x] 캔버스를 우선하고 작업별 왼쪽/오른쪽 패널을 하나씩 열 수 있게 구현한다.
  - [x] 800×844 경계와 390×844에서 Playwright 브라우저 상태로 검증한다.
- 수용 기준:
  - [x] 좁은 화면에서 캔버스가 양쪽 패널에 동시에 가려지지 않는다.
  - [x] 모바일 UA의 기존 지원 불가 안내를 유지한다.

### UX-07 — Segmentation 첫 입력의 클래스 의도 명확화

- 우선순위: P2
- 상태: `Verified`
- 의존성: D-03
- 작업 체크:
  - [x] D-03 결정을 기록한다.
  - [x] 빈 마스크에서는 명시적 클래스 선택 전 추가 페인트 도구 입력을 차단한다.
  - [x] 클래스 메타데이터 유무에 따라 클래스 선택 또는 클래스 파일 로드/생성을 안내한다.
  - [x] 첫 브러시·Undo E2E와 워크플로 단위 테스트를 보강한다.
- 수용 기준:
  - [x] 사용자가 첫 페인트 전에 기록될 클래스를 알 수 있다.
  - [x] 그리기 후 Undo/Redo가 일관되게 동작한다.

### QA-01 — 기존 E2E 실패 분류 및 복구

- 우선순위: P1
- 상태: `Verified`
- 의존성: UX-01
- 최종 결과: 21개 모두 통과. 공통 원인은 샘플 시작 이미지였고, 독립 3건은 테스트 격리·좌표·기대값 결함으로 판정해 수정했다.
- 작업 체크:
  - [x] 깨끗한 단독 실행으로 실패를 재현한다.
  - [x] 샘플 초기 상태 공통 원인과 독립 실패를 분리한다.
  - [x] 매칭 워커 오류 상태, Segmentation Undo, workflow switching을 테스트 결함으로 판정한다.
  - [x] 워커 격리, 캔버스 밖 좌표, 현재 클래스 목록 기대값을 수정한다.
- 수용 기준:
  - [x] 모든 실패에 원인, 소유 작업, 재현 명령이 연결돼 있다.
  - [x] 전체 E2E가 통과한다.

### 2026-09-12 — UX-01/02/03/04, QA-01

- 상태: `Verified`
- 변경 범위: 샘플 시작 이미지, Review 진입·대기열 위치, Automation 레이아웃 미리보기, Detection 객체 목록, E2E 격리
- 검증:
  - `npm run typecheck` — PASS
  - `npm run test:unit` — PASS, 342/342
  - `npm run build` — PASS
  - `npm run test:e2e` — PASS, 21/21
- E2E 실패 분류:
  - 샘플 관련 5건: 자연 정렬의 첫 파일과 데모 시작 이미지가 달랐던 제품 결함 → UX-01에서 수정
  - 매칭 시작 실패: 테스트가 Template Matching뿐 아니라 EdgeSAM Worker까지 차단한 격리 결함 → 대상 Worker만 실패하도록 수정
  - Segmentation Undo: 이미지 밖 검증 좌표가 상단 AI Select 버튼과 겹친 테스트 좌표 결함 → 하단 여백으로 이동
  - Workflow switching: 숨겨진 작업 버튼 클릭과 클래스 파일 없는 데이터의 과거 기본 클래스 기대 → 상단 워크플로 탭과 현재 계약으로 수정
- 재현 명령:
  - `npx playwright test tests/e2e/bootstrap-smoke.spec.ts tests/e2e/segmentation-draw.spec.ts tests/e2e/workflow-switching.spec.ts --project=chromium`
- 다음 작업: UX-05, UX-06, UX-07 진행 → 아래 후속 기록에서 완료
- 막힘 또는 결정 필요: D-01, D-02, D-03 → 사용자 승인으로 해소

### 2026-09-12 — UX-05/06/07

- 상태: `Verified`
- 승인 결정: `D-01 A, D-02 A, D-03 A`
- 변경 범위: 전처리 패널 맥락, 800px 이하 단일 패널 상태, Segmentation 첫 클래스 선택 계약
- 완료한 체크 항목:
  - [x] Preprocessing 진입 시 오른쪽 Inspector를 닫고 입력 소스 관계를 안내한다.
  - [x] 800px 이하에서 작업 전환·수동 확장 시 왼쪽/오른쪽 패널이 상호 배타적으로 열린다.
  - [x] 빈 마스크에서 클래스 선택 전 Brush·Polygon·Superpixel·AI Select 추가 입력을 차단한다.
  - [x] 클래스 `0`은 배경/지우기 값으로 유지하고 페인트 클래스 목록에서 제외한다.
  - [x] 클래스 메타데이터가 없으면 클래스 파일 로드/생성 안내를 표시한다.
- 검증:
  - `npm run typecheck` — PASS
  - `npm run test:unit` — PASS, 343/343
  - `npm run build` — PASS
  - `npm run test:e2e` — PASS, 21/21
  - `workflow-switching.spec.ts` — 800×844 및 390×844에서 한 패널만 열리는 상태 확인
- 다음 작업: 없음 — 감사 보고서의 P1/P2 후속 항목 완료
- 막힘 또는 결정 필요: 없음

## 매 작업 시 진척도 갱신 형식

```md
### YYYY-MM-DD — 작업 ID

- 상태: Not started → In progress / Done / Verified / Blocked
- 변경 범위: 파일 또는 기능
- 완료한 체크 항목:
  - [x] ...
- 검증:
  - 명령: `...`
  - 결과: PASS / FAIL
- 다음 작업:
  - ...
- 막힘 또는 결정 필요:
  - ...
```

## 최종 검증 체크리스트

- [x] 빈 작업공간 → 샘플 로드 → Detection 주석 확인
- [x] Detection → Review 진입 → 첫 검수 항목 처리 → 상태 저장
- [x] Detection → Automation 진입 → 선택 레이아웃 미리보기 확인
- [x] Detection → Preprocessing 전환 → 현재 목적과 일치한 보조 패널 확인
- [x] Segmentation → 신규 이미지 → 클래스 선택 → 그리기 → Undo/Redo
- [x] 최소 지원 폭 및 390×844 정책 확인
- [x] `npm run test:unit`
- [x] `npm run test:e2e`
